export interface Project {
  id: string,
  title: string,
  gauge?: string,
  yarn?: string,
  needles?: string,
  description?: string,
  startDate: number,
  lastEdit: number,
  lastSectionId: string,
}
